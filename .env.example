# ─── LLM Provider ────────────────────────────────────────────────────────────
# LLM provider selection: openai | anthropic
# If not set, auto-selects (OpenAI if OPENAI_API_KEY is present, otherwise Anthropic)
LLM_PROVIDER=openai

# ─── OpenAI ──────────────────────────────────────────────────────────────────
# Required when LLM_PROVIDER=openai or not set
# Default model: gpt-4o
OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o

# ─── Anthropic ───────────────────────────────────────────────────────────────
# Required when LLM_PROVIDER=anthropic
# Default model: claude-opus-4-6
# ANTHROPIC_API_KEY=
# ANTHROPIC_MODEL=claude-opus-4-6

# ─── Web Search (optional) ────────────────────────────────────────────────────
# If Brave Search API key is set, Brave is used first; otherwise falls back to DuckDuckGo
# BRAVE_SEARCH_API_KEY=
