# ─── LLM Provider ────────────────────────────────────────────────────────────
# LLM provider selection: openai | anthropic
# If not set, auto-selects (OpenAI if OPENAI_API_KEY is present, otherwise Anthropic)
LLM_PROVIDER=openai

# ─── OpenAI ──────────────────────────────────────────────────────────────────
# Required when LLM_PROVIDER=openai or not set
# Default model: gpt-4o
OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o

# ─── Anthropic ───────────────────────────────────────────────────────────────
# Required when LLM_PROVIDER=anthropic
# Default model: claude-opus-4-6
# ANTHROPIC_API_KEY=
# ANTHROPIC_MODEL=claude-opus-4-6

# ─── Web Search (optional) ────────────────────────────────────────────────────
# If Brave Search API key is set, Brave is used first; otherwise falls back to DuckDuckGo
# BRAVE_SEARCH_API_KEY=

# ─── Notifications (optional) ─────────────────────────────────────────────────
# When the user says "let me know" / "notify me", messages are delivered via:
# 1. Telegram (if both vars set)  2. Slack (if webhook set)  3. Memory log fallback
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_CHAT_ID=
# SLACK_WEBHOOK_URL=

# ─── Default User (optional) ──────────────────────────────────────────────────
# Applied only when the user record is first created (fresh database).
# Changing these after first run has no effect — update via Settings UI instead.
# DEFAULT_USER_NAME=User
# DEFAULT_USER_PREFERRED_NAME=
# DEFAULT_USER_LOCALE=en
# DEFAULT_USER_TIMEZONE=UTC
# DEFAULT_USER_INTERESTS=coding,music,travel

# ─── Default Agent (optional) ─────────────────────────────────────────────────
# Applied only when the agent identity note is first initialized.
# Changing these after first run has no effect — use the "update_soul" tool instead.
# DEFAULT_AGENT_NAME=Lumi
# DEFAULT_AGENT_PERSONALITY=Helpful, thoughtful, and direct. Curious about the world and eager to assist.
# DEFAULT_AGENT_STYLE=Conversational and warm, but concise. Gets to the point without being curt.
